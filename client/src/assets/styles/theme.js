import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#eee",
      contrastText: "#181818",
    },
    secondary: {
      main: "#181818",
      contrastText: "#eee",
    },
  },
});
